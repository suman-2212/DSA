class Solution:
    def minSubset(self, arr):
        arr.sort(reverse=True)
        total = sum(arr)
        curr = 0

        for i in range(len(arr)):
            curr += arr[i]
            total -= arr[i]
            if curr > total:
                return i + 1
